name: jammy.ubuntu.kubevirt
type: img
env: {}
next:
- build: docker
  run_when:
    - release
  config:
    base_image: scratch
    image_name: eons/jammy.ubuntu.kubevirt.img
    also:
      - ADD --chown=107:107
        https://cloud-images.ubuntu.com/jammy/current/jammy-server-cloudimg-amd64.img
        /disk/
    tags:
      - latest
